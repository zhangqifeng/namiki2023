<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thymeleaf.dao.EmployeeDao">
    <select id="lists" resultType="Employee">
        select * from `employee`
    </select>
    <insert id="save" parameterType="Employee">
        insert into `employee`(employee_id,employee_name,sex,address,department,job_title,employment_status,hire_date)
        values(#{employee_id},#{employee_name},#{sex},#{address},#{department},#{job_title},#{employment_status},#{hire_date})
    </insert>
    <select id="findById" parameterType="Integer" resultType="Employee">
        select * from `employee` where employee_id=#{employee_id}
    </select>
    <update id="update" parameterType="map">
        update `employee` set employee_id=#{employee_id},employee_name=#{employee_name},sex=#{sex},address=#{address},department=#{department},
                              job_title=#{job_title},employment_status=#{employment_status},hire_date=#{hire_date}
        where id=#{id}
    </update>
    <delete id="delete" parameterType="Integer">
        delete from `employee` where employee_id=#{employee_id}
    </delete>
    <select id="search" parameterType="map" resultType="Employee">
        SELECT * FROM `employee`
        <where>
            <if test="employee_id!=null and employee_id!='' ">
                AND employee_id=#{employee_id}
            </if>
            <if test="employee_name!=null and employee_name !='' ">
                AND employee_name LIKE concat('%',#{employee_name},'%')
            </if>
            <if test="department!=null and department!='' ">
                AND department LIKE concat('%',#{department},'%')
            </if>
        <if test="address!=null and address!='' ">
            AND address LIKE concat('%',#{address},'%')
        </if>
        </where>
    </select>
</mapper>